include "stdio.h";

function main(): Int {
    var a = 0;
    var b = 0;
    var t = (5, 6);
    (a, b) = t;
    printf("%d %d\n", a, b);
    (a, b) = (7, 8);
    printf("%d %d\n", a, b);
    return 0;
}
