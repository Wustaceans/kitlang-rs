include "stdio.h";

function main() {
    var x: Int = 10;
    printf("initial: %d\n", x);

    x += 5;
    printf("after += 5: %d\n", x);

    x -= 3;
    printf("after -= 3: %d\n", x);

    x *= 2;
    printf("after *= 2: %d\n", x);

    x /= 4;
    printf("after /= 4: %d\n", x);
}
